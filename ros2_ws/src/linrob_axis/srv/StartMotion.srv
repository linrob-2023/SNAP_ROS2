# Service for starting a motion to a target position
bool start            # Set true to trigger motion command (e.g. GVL/xVirtMove)
float64 target_position # Target position identifier / index
---
bool success
string message
